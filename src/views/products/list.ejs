<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800">
  <div class="min-h-screen grid grid-cols-12">
    <aside class="col-span-12 md:col-span-3 lg:col-span-2 bg-white border-r p-4 space-y-2">
      <h1 class="text-xl font-bold text-indigo-600">WearConnect Admin</h1>
      <nav class="mt-4 space-y-1">
        <a class="block px-3 py-2 rounded hover:bg-gray-100" href="/admin">Dashboard</a>
        <a class="block px-3 py-2 rounded hover:bg-gray-100" href="/admin/categories">Categories</a>
        <a class="block px-3 py-2 rounded hover:bg-gray-100" href="/admin/uploads">Uploads</a>
      </nav>
    </aside>
    <main class="col-span-12 md:col-span-9 lg:col-span-10 p-6">
      <div class="flex items-center justify-between mb-6">
        <div>
          <h2 class="text-2xl font-semibold"><%= category.name %> • Products</h2>
          <div class="text-sm text-gray-500">Slug: <code><%= category.slug %></code></div>
        </div>
        <a href="/admin/categories/<%= category.slug %>/products/new" class="px-4 py-2 bg-indigo-600 text-white rounded hover:bg-indigo-700">New Product</a>
      </div>

      <!-- Toast -->
      <div id="toast" class="fixed top-4 right-4 hidden px-4 py-3 rounded-md text-white shadow-lg z-50"></div>

      <!-- Card Grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5">
        <% (products||[]).forEach(p => { %>
          <div class="group bg-white border rounded-2xl shadow-sm hover:shadow-md transition overflow-hidden">
            <div class="h-40 bg-gray-100 overflow-hidden">
              <% if (p.image) { 
                   let img = (p.image || '').replace(/\\/g,'/');
                   if (img && img[0] !== '/') img = '/' + img;
                   // Prefer backend uploads; map /images/* to /uploads/*
                   if (img.startsWith('/images/')) img = img.replace('/images/', '/uploads/');
                   // If looks like "/filename.ext" (no folder), prefix /uploads/
                   if (!img.startsWith('/uploads/') && img.split('/').length === 2) {
                     img = img.replace(/^\//, '/uploads/');
                   }
              %>
                <img src="<%= img %>" alt="<%= p.name %>" class="w-full h-full object-cover group-hover:scale-105 transition duration-300" />
              <% } else { %>
                <div class="w-full h-full flex items-center justify-center text-gray-400 text-sm">No Image</div>
              <% } %>
            </div>
            <div class="p-4">
              <div class="text-sm text-gray-500 mb-1"><code><%= p.id %></code></div>
              <div class="font-semibold"><%= p.name %></div>
              <div class="mt-3 flex items-center justify-between text-sm">
                <a class="px-3 py-1 border rounded hover:bg-gray-50" href="/admin/categories/<%= category.slug %>/products/<%= p.id %>/edit">Edit</a>
                <button class="px-3 py-1 border border-red-300 text-red-700 rounded hover:bg-red-50" data-slug="<%= category.slug %>" data-id="<%= p.id %>" data-name="<%= p.name %>" onclick="confirmDelete(this.dataset.slug,this.dataset.id,this.dataset.name)">Delete</button>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    </main>
  </div>

  <!-- Centered Confirm Modal -->
  <div id="confirmModal" class="fixed inset-0 bg-black/40 items-center justify-center hidden">
    <div class="bg-white rounded-xl p-6 w-[90%] max-w-md shadow-xl">
      <h3 class="text-lg font-semibold">Delete Product</h3>
      <p id="confirmText" class="mt-2 text-sm text-gray-600"></p>
      <div class="mt-4 flex items-center justify-end gap-2">
        <button class="px-3 py-2 rounded border" onclick="hideModal()">Cancel</button>
        <form id="deleteForm" method="post">
          <button class="px-3 py-2 rounded bg-red-600 text-white hover:bg-red-700">Delete</button>
        </form>
      </div>
    </div>
  </div>

  <script>
    const toast = (msg, type='success') => {
      const el = document.getElementById('toast');
      el.textContent = msg;
      el.className = `fixed top-4 right-4 px-4 py-3 rounded-md text-white shadow-lg z-50 ${type==='success' ? 'bg-emerald-600' : 'bg-rose-600'}`;
      el.style.opacity = 0;
      el.style.display = 'block';
      el.animate([{ opacity: 0 }, { opacity: 1 }], { duration: 200 });
      setTimeout(() => {
        el.animate([{ opacity: 1 }, { opacity: 0 }], { duration: 200 });
        setTimeout(()=>{ el.style.display='none'; }, 200);
      }, 1500);
    };

    function confirmDelete(slug, id, name) {
      const modal = document.getElementById('confirmModal');
      document.getElementById('confirmText').textContent = `Are you sure you want to delete "${name}"?`;
      const form = document.getElementById('deleteForm');
      form.action = `/api/admin/categories/${slug}/products/${id}/delete`;
      modal.classList.remove('hidden');
      modal.classList.add('flex');
    }
    function hideModal() {
      const modal = document.getElementById('confirmModal');
      modal.classList.add('hidden');
      modal.classList.remove('flex');
    }
  </script>
</body>
</html>
