<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || 'Admin Login' %> • WearConnect</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800 min-h-screen grid place-items-center p-4 relative">
  <!-- Centered toast -->
  <div id="toast" class="hidden fixed top-6 left-1/2 -translate-x-1/2 z-50 px-4 py-3 rounded-md text-white shadow-lg"></div>

  <div class="w-full max-w-sm bg-white rounded-2xl shadow p-6 ring-1 ring-gray-200">
    <div class="mb-5 text-center">
      <div class="mx-auto w-10 h-10 rounded-lg bg-indigo-600 text-white flex items-center justify-center text-xl font-bold">W</div>
      <h1 class="mt-3 text-xl font-semibold">Admin Login</h1>
      <p class="text-sm text-gray-500">Sign in to access the admin dashboard</p>
    </div>
    <form method="post" action="/api/auth/login-web" class="space-y-3">
      <div>
        <label class="text-sm text-gray-600">Email</label>
        <input required name="email" type="email" class="mt-1 w-full border rounded-md px-3 py-2" placeholder="admin@example.com" />
      </div>
      <div>
        <label class="text-sm text-gray-600">Password</label>
        <input required name="password" type="password" class="mt-1 w-full border rounded-md px-3 py-2" placeholder="••••••••" />
      </div>
      <button class="w-full px-4 py-2 bg-black text-white rounded-md hover:bg-gray-800">Sign In</button>
    </form>
    <div class="mt-4 text-xs text-gray-500">
      <p>Tip: Seed an admin via API if none exists:</p>
      <pre class="mt-1 bg-gray-50 p-2 rounded">POST /api/auth/seed-admin { email, password }</pre>
    </div>
  </div>

  <script>
    const params = new URLSearchParams(location.search);
    if (params.get('unauth') === '1') {
      const t = document.getElementById('toast');
      t.textContent = 'Please sign in to continue';
      t.className = 'fixed top-6 left-1/2 -translate-x-1/2 z-50 px-4 py-3 rounded-md text-white shadow-lg bg-rose-600';
      t.style.opacity = 0; t.style.display = 'block';
      t.animate([{opacity:0},{opacity:1}],{duration:200});
      setTimeout(()=>{ t.animate([{opacity:1},{opacity:0}],{duration:250}); setTimeout(()=>{ t.style.display='none'; },250); }, 1500);
    }
  </script>
</body>
</html>
